# VFX Credits Filter Service

Веб-сервис для извлечения и фильтрации VFX-специалистов из кредитов фильмов через TMDb API.

## 🎯 Возможности

- 📂 Загрузка CSV/Excel таблиц с данными о фильмах
- 🔍 Поиск фильмов по IMDB ID или названию
- 🎬 Автоматическое извлечение кредитов через TMDb API
- 🎨 Фильтрация только VFX-специалистов (продюсеры, супервайзеры, композеры и т.д.)
- 📊 Отображение результатов в удобной таблице
- 📥 Экспорт результатов в Excel
- 🐳 Docker-контейнер для простого развертывания с поддержкой обогащения LinkedIn

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- TMDb API ключ (получить на https://www.themoviedb.org/settings/api)
- Аккаунт LinkedIn с валидными cookie или логином/паролем для библиотеки [`linkedin-api`](https://github.com/nsandman/linkedin-api)

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd vfx-cred
```

### 2. Настройка окружения

Создайте файл `.env` из примера:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваш TMDb API ключ и реквизиты LinkedIn:

```env
TMDB_API_KEY=ваш_ключ_здесь
TMDB_BASE_URL=https://api.themoviedb.org/3
HOST=0.0.0.0
PORT=8010
LINKEDIN_USERNAME=адрес_вашей_почты_в_LinkedIn
LINKEDIN_PASSWORD=пароль_от_LinkedIn
# либо укажите cookie, сохранённые через linkedin-api
# LINKEDIN_COOKIES_PATH=/app/config/linkedin_cookies.json
# LINKEDIN_COOKIES_JSON={"li_at":"...","JSESSIONID":"..."}
```

### 3. Запуск через Docker Compose

```bash
docker-compose up -d
```

Сервис будет доступен по адресу: http://localhost:8010

### 4. Проверка работоспособности

```bash
curl http://localhost:8010/api/health
```

## 📖 Использование

### Веб-интерфейс

1. Откройте браузер и перейдите на http://localhost:8010
2. Выберите один из режимов:
   - **Загрузить таблицу**: для массовой обработки фильмов
   - **Поиск по фильму**: для поиска одного фильма
3. Убедитесь, что в таблице появилась колонка LinkedIn, если заданы учётные данные LinkedIn

### LinkedIn обогащение через linkedin-api

- Модуль `services.linkedin_lookup` использует неофициальную библиотеку [`linkedin-api`](https://github.com/nsandman/linkedin-api), которая требует действующие сессионные cookie или логин/пароль реального аккаунта.
- Данные для авторизации передаются через переменные окружения `LINKEDIN_USERNAME` и `LINKEDIN_PASSWORD` либо через JSON с cookie (`LINKEDIN_COOKIES_JSON` или `LINKEDIN_COOKIES_PATH`).
- Если переменные не заданы, колонка LinkedIn останется пустой и запросы к LinkedIn выполняться не будут.

#### Кастомизация

| Переменная | Назначение | Значение по умолчанию |
|------------|------------|-----------------------|
| `LINKEDIN_USERNAME` / `LINKEDIN_PASSWORD` | Основная авторизация | — |
| `LINKEDIN_COOKIES_PATH` / `LINKEDIN_COOKIES_JSON` | Альтернатива логину/паролю | — |
| `LINKEDIN_PROXY` (`LINKEDIN_HTTP_PROXY`, `LINKEDIN_HTTPS_PROXY`) | Прокси для обхода блокировок | — |
| `LINKEDIN_REQUEST_INTERVAL` | Пауза между запросами (сек.) | `1.5` |
| `LINKEDIN_MAX_RESULTS` | Сколько профилей анализировать на один запрос | `3` |
| `LINKEDIN_CONCURRENCY` | Параллельные поиски | `1` |

Все переменные уже проброшены в `docker-compose.yml`, поэтому достаточно обновить `.env`.

#### Как получить cookie или сохранить сессию

1. Локально установите пакет: `pip install linkedin-api`.
2. Выполните любой Python-скрипт, который создаёт сессию и сохраняет cookie в файл:

   ```python
   import json
   from linkedin_api import Linkedin

   api = Linkedin("email@example.com", "ваш_пароль")
   with open("linkedin_cookies.json", "w", encoding="utf-8") as fp:
       json.dump(api.client.session.cookies.get_dict(), fp)
   ```

3. Скопируйте полученный файл в безопасное место и укажите путь в переменной `LINKEDIN_COOKIES_PATH` (или вставьте содержимое в `LINKEDIN_COOKIES_JSON`).
4. При необходимости обновляйте cookie, если LinkedIn завершает сессию.

#### Загрузка таблицы

1. Подготовьте CSV или Excel файл с колонками:
   - Колонка с IMDB ссылками (например, "IMDB URL", "Link")
   - Колонка с названиями фильмов (например, "Title", "Film Name")

Пример таблицы:
```csv
Title,IMDB URL
The Matrix,https://www.imdb.com/title/tt0133093/
Inception,tt0468569
```

2. Перетащите файл в область загрузки или нажмите для выбора
3. Нажмите "Обработать файл"
4. Дождитесь обработки (может занять время для большого количества фильмов)
5. Просмотрите результаты в таблице
6. При необходимости отфильтруйте результаты
7. Экспортируйте в Excel

#### Поиск по фильму

1. Введите IMDB ID (например, `tt0133093`) или название фильма
2. Нажмите "Найти"
3. Просмотрите найденных VFX-специалистов

### API Endpoints

#### Health Check
```bash
GET /api/health
```

Ответ:
```json
{
  "status": "healthy",
  "tmdb_api_configured": true
}
```

#### Загрузка CSV/Excel
```bash
POST /api/upload-csv
Content-Type: multipart/form-data

file: <ваш_файл.csv или .xlsx>
```

Ответ:
```json
{
  "success": true,
  "processed_movies": [...],
  "vfx_crew": [...],
  "total_vfx_crew": 150
}
```

#### Поиск фильма
```bash
POST /api/search-movie
Content-Type: application/json

{
  "imdb_id": "tt0133093",
  "title": "The Matrix"
}
```

Ответ:
```json
{
  "success": true,
  "movie": {
    "title": "The Matrix",
    "imdb_id": "tt0133093",
    "tmdb_id": "603",
    "overview": "...",
    "release_date": "1999-03-31"
  },
  "vfx_crew": [...],
  "total_vfx_crew": 25
}
```

#### Экспорт в Excel
```bash
POST /api/export
Content-Type: application/json

[
  {
    "name": "John Gaeta",
    "job": "Visual Effects Supervisor",
    "department": "Visual Effects",
    "movie_title": "The Matrix",
    "imdb_id": "tt0133093"
  },
  ...
]
```

Ответ: Excel файл (download)

## 🔧 Локальная разработка (без Docker)

### Установка зависимостей

```bash
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
```

### Запуск сервера

```bash
python app.py
```

Или через uvicorn:

```bash
uvicorn app:app --host 0.0.0.0 --port 8000 --reload
```

## 🌐 Развертывание на сервере (cred.ai-vfx.com)

### С использованием Docker

1. Скопируйте файлы на сервер:
```bash
scp -r . user@cred.ai-vfx.com:/opt/vfx-credits/
```

2. Подключитесь к серверу:
```bash
ssh user@cred.ai-vfx.com
cd /opt/vfx-credits/
```

3. Настройте `.env` файл с вашим TMDb API ключом

4. Запустите контейнер:
```bash
docker-compose up -d
```

### Настройка Nginx (реверс-прокси)

Создайте конфигурацию Nginx для домена `cred.ai-vfx.com`:

```nginx
server {
    listen 80;
    server_name cred.ai-vfx.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Для HTTPS с Let's Encrypt:

```bash
sudo certbot --nginx -d cred.ai-vfx.com
```

### Systemd Service (альтернатива Docker Compose)

Создайте файл `/etc/systemd/system/vfx-credits.service`:

```ini
[Unit]
Description=VFX Credits Filter Service
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/vfx-credits
Environment="PATH=/opt/vfx-credits/venv/bin"
ExecStart=/opt/vfx-credits/venv/bin/python -m uvicorn app:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl enable vfx-credits
sudo systemctl start vfx-credits
```

## 🎨 Фильтруемые VFX должности

Сервис фильтрует следующие должности:
- Visual Effects Supervisor/Producer/Coordinator
- VFX Supervisor/Producer/Coordinator
- Compositor
- Animator
- CG Artist
- 3D Artist
- Effects Artist
- Digital Artist
- Matte Painter
- Rotoscoping Artist
- Tracking Artist
- Lighting Artist
- Rendering Artist
- FX Artist

## 📊 Структура проекта

```
vfx-cred/
├── app.py                  # Основное приложение FastAPI
├── requirements.txt        # Python зависимости
├── Dockerfile             # Docker образ
├── docker-compose.yml     # Docker Compose конфигурация
├── .env.example           # Пример файла окружения
├── .gitignore            # Git ignore правила
├── README.md             # Документация
└── static/
    └── index.html        # Веб-интерфейс
```

## 🔑 Получение TMDb API ключа

1. Зарегистрируйтесь на https://www.themoviedb.org/
2. Перейдите в Settings → API
3. Запросите API ключ (выберите Developer)
4. Заполните форму заявки
5. Получите API ключ и добавьте в `.env`

## 🐛 Решение проблем

### Ошибка "TMDB API key not configured"

Убедитесь, что в `.env` файле установлен `TMDB_API_KEY`

### Ошибка "Movie not found"

- Проверьте правильность IMDB ID
- Убедитесь, что фильм есть в базе TMDb
- Попробуйте поиск по названию вместо IMDB ID

### Контейнер не запускается

Проверьте логи:
```bash
docker-compose logs -f
```

### Медленная обработка больших таблиц

Это нормально - API запросы к TMDb занимают время. Для оптимизации можно:
- Разбить таблицу на меньшие части
- Добавить кэширование результатов (требует модификации кода)

## 📝 Лицензия

MIT License

## 🤝 Контакты

По вопросам и предложениям создавайте issue в репозитории.

## 🔄 Обновление

Для обновления сервиса:

```bash
git pull
docker-compose down
docker-compose build
docker-compose up -d
```

## 📈 Планы развития

- [ ] Кэширование результатов запросов к TMDb
- [ ] Пакетная обработка с прогресс-баром
- [ ] Фильтрация по конкретным должностям
- [ ] Поддержка других API (IMDB, OMDB)
- [ ] Статистика по студиям и годам
- [ ] Экспорт в различные форматы (CSV, JSON)
- [ ] Аутентификация пользователей
- [ ] История запросов
